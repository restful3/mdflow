# mdflow M0 Skeleton Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the M0 skeleton of mdflow — core types, registry, cache, URL fetch helper, text passthrough converter, runtime capabilities, FastAPI app with `/healthz` and admin endpoints — so that subsequent milestones (M1 office formats, M2 PDF, etc.) plug into a working chassis.

**Architecture:** Python src layout (`src/mdflow/`). Strict TDD per task. Core modules (`errors`, `events`, `format_detect`, `url_fetch`, `cache`, `registry`, `service`) are pure-Python + Pydantic. FastAPI app wires `ConversionService` via lifespan-initialized `Capabilities`. M0 ships the chassis without `/convert` SSE handler (that lands in M1); `ConversionService` is fully wired and exercised through direct calls + a smoke test.

**Tech Stack:** Python ≥ 3.11, FastAPI, Pydantic v2, Pydantic-Settings, httpx, python-magic, chardet, pytest, pytest-asyncio, ruff.

---

## TL;DR — 단계 / 범위 / 리스크

**근거 문서**: PRD §14 마일스톤 표 + URL 처리 합의안(`docs/reviews/2026-05-21-url-handling-final-agreement.md`). URL 합의의 7개 v1 원칙(범용 게이트웨이 / SSRF v1 필수 / 사용자 헤더·쿠키 차단 / cache key bytes 기준 / quality gate v1.1 / Headless v1.1 / DOI pre-resolve 미도입)은 **Task 4 (Settings)** + **Task 12 (URL fetch helper)** + **Task 13 (Service)** 에 그대로 반영된다.

### 단계 (Phase A → G, 17 task TDD)

| Phase | Tasks | 핵심 산출물 |
|---|---|---|
| A. Bootstrap & core types | 1, 2, 3 | `pyproject.toml`, `ErrorCode`(15개 — `URL_*` 7개 포함), Pydantic 이벤트 모델 6종 |
| B. Settings & detection & converter base | 4, 5, 6 | `MDFLOW_*` 환경변수 9개 + URL-input cap 검증, `detect_format` magic 우선, `Converter` Protocol |
| C. Text & registry & cache | 7, 8, 9 | txt/md/csv 패스스루(chardet), 데코레이터 디스패치, sha256 atomic 디스크 캐시 |
| D. Runtime | 10, 11 | `Capabilities`(GPU detect + 부팅 로그), `ConcurrencyPool`(GPU 세마포어=1 + CPU 풀) |
| E. URL fetch helper ★ | 12 | `validate_url`+SSRF(IPv4·IPv6·metadata·redirect별 재검증)+size cap 스트리밍+`FetchResult` |
| F. Service & API | 13, 14, 15 | `ConversionService` 캐시 통합, FastAPI 앱+lifespan+`/healthz`, `/capabilities`+`/cache/*` |
| G. Smoke & tag | 16, 17 | 통합 smoke 테스트, ruff clean, `v0.0.1-m0` 태그 |

### 범위 — In / Out (M0 한정)

**In:**
- 패키지 골격 (`src/mdflow/`), 빌드 설정, 테스트 인프라
- 핵심 타입: `ErrorCode`, 이벤트 모델, `Converter` Protocol, `ConversionContext`/`Result`
- `Registry`(데코레이터 등록 + 디스패치), `Cache`(sha256 atomic 쓰기 + 통계 + purge)
- 텍스트 패스스루 컨버터(txt/md/csv) — M0의 유일한 컨버터
- 런타임: GPU 자동 감지 + `MDFLOW_FORCE_CPU` override, 부팅 로그 한 줄
- **URL fetch helper** (합의안 §3.2의 1\~10단계) — `/convert`가 M1이라도 helper는 M0에서 완성
- HTTP API: `/healthz`, `/capabilities`, `/cache/{sha}` GET/DELETE, `/cache/purge`

**Out (다음 마일스톤):**
- `/convert` SSE 핸들러 → M1
- 사무 포맷 컨버터(docx/pptx/xlsx/html) → M1
- PDF(Marker/PyMuPDF) → M2
- LibreOffice 폴백, hwp → M3
- MCP transport (stdio + Streamable HTTP) → M4
- CLI, Dockerfile, 메트릭 → M5

**합의안에 의해 명시적으로 v1에서 제외된 항목 (Task로 만들지 않음)**:
- 사용자 헤더/쿠키/Authorization 전달 (Task 12에서 차단 로직만 검증)
- `follow_redirects=false` 옵션
- 키워드 기반 bot/error hard reject
- Headless browser, 학술 사이트 transformer, DOI pre-resolve, 인증 fetch

### 리스크 (M0 실행 중 부딪힐 가능성이 높은 순)

| # | 리스크 | 영향 | 완화/판단 |
|---|---|---|---|
| R1 | **libmagic 시스템 의존성** — `python-magic`은 OS의 `libmagic` 라이브러리 필요. CI/일부 배포 환경에서 미설치 가능 | `_magic_format()` 실패 시 prefix probe만 사용 → M0 fixture(`%PDF`, ZIP OOXML, HTML) cover율은 충분하나 다른 포맷에서 격차 발생 | M0 fixture로 cover된 범위는 prefix probe로 자급. CI 환경에 `libmagic` 설치 권고를 docs/dev README에 명시 (Task 1) |
| R2 | **SSRF DNS rebinding** — `socket.getaddrinfo` 검사 시점과 실제 TCP 연결 시점 사이에 DNS 응답이 바뀌면 우회 가능 | private IP로의 우회 fetch | M0는 best-effort getaddrinfo + IP literal 직접 검사로 1차 차단. 완전 차단은 socket 레벨 IP-pinned connection 필요 → PRD §13 "도메인 allowlist/blocklist" v1.1 항목에 흡수 |
| R3 | **`asyncio.Semaphore`의 이벤트 루프 바인딩** — `ConcurrencyPool`을 lifespan 밖(예: 직접 `__init__`)에서 만들면 lifespan의 루프와 다른 루프에 묶여 deadlock 위험 | `/convert` 호출 시 비정상 hang | Task 11 + Task 14 lifespan에서 `ConcurrencyPool(cpu_workers=...)`을 **lifespan async 컨텍스트 내부**에서만 생성. 테스트도 `asyncio.run()` 안에서 인스턴스화 |
| R4 | **`chardet` 짧은 텍스트 정확도** — 20자 미만이나 ASCII 안전 영역만 있는 텍스트에서 한국어/일본어/중국어 인코딩을 정확히 못 잡을 수 있음 | 한국어 짧은 텍스트가 `latin-1` 폴백으로 떨어져 mojibake | M0 acceptable. 사용자가 `language_hint` 옵션(M1 추가 예정)으로 보강 가능. fixture는 UTF-8 + cp949 둘 다 |
| R5 | **`urlparse`의 관대함** — Python `urlparse`는 일부 mangled URL(예: schema 누락, 비표준 host 형태)을 silently 받음 | `validate_url()`이 통과한 URL이 httpx에서 실패하거나, 우회 가능 | Task 12 테스트는 명시적 negative case 13개로 cover. 추가 강화(IDNA·trailing dot 정규화)는 합의안에서 v1.1로 분류됨 |
| R6 | **캐시 동시 쓰기 경합** — 같은 sha에 두 요청이 동시에 미스로 들어가면 마지막 `os.replace`만 승 | 변환 결과는 deterministic이므로 실질 영향 없음 | M0 acceptable. M1 이후 high-throughput 시 file lock 또는 in-memory pending dict 추가 검토 |

### 실행 시 체크포인트

- **Task 6 직후**: `Converter` Protocol이 PRD §4.1 시그니처와 동일한지 비교 (이름·타입·인자 순서)
- **Task 9 직후**: 캐시 키가 PRD §9의 `sha256(input_bytes || canonical_options_json)`과 일치하는지 sha 직접 계산 한 번 (`echo` + `python -c`)
- **Task 12 직후**: 합의안 §3.2의 10단계가 모두 코드/테스트에 매핑되는지 체크리스트로 확인
- **Task 17 직후**: `git log --oneline` 으로 17 commit + 1 tag 확인

---

## File Structure

**New files (M0 scope):**

| Path | Responsibility |
|---|---|
| `pyproject.toml` | Project metadata, dependencies, ruff/pytest config |
| `src/mdflow/__init__.py` | Package marker, `__version__` |
| `src/mdflow/core/__init__.py` | Subpackage marker |
| `src/mdflow/core/errors.py` | `ErrorCode` enum (15 codes incl. URL_*), `MdflowError` exception |
| `src/mdflow/core/events.py` | Pydantic SSE event models (Started/Progress/Done/Error/Cached/Queued) |
| `src/mdflow/core/format_detect.py` | Extension + magic-bytes detection with disagreement reporting |
| `src/mdflow/core/url_fetch.py` | URL validation, SSRF policy, redirect-aware fetch with size cap |
| `src/mdflow/core/cache.py` | sha256 disk cache (atomic write + read + TTL helper) |
| `src/mdflow/core/registry.py` | Decorator-based converter registry + dispatch |
| `src/mdflow/core/service.py` | `ConversionService` — context build, cache lookup, dispatch |
| `src/mdflow/converters/__init__.py` | Subpackage marker |
| `src/mdflow/converters/base.py` | `Converter` Protocol, `ConversionContext`, `ConversionResult` |
| `src/mdflow/converters/text.py` | txt/md/csv passthrough + encoding detect |
| `src/mdflow/runtime/__init__.py` | Subpackage marker |
| `src/mdflow/runtime/capabilities.py` | `Capabilities` (GPU detect, env override, boot log line) |
| `src/mdflow/runtime/concurrency.py` | GPU semaphore (=1) + CPU thread pool |
| `src/mdflow/api/__init__.py` | Subpackage marker |
| `src/mdflow/api/app.py` | FastAPI factory, lifespan |
| `src/mdflow/api/admin.py` | `/healthz`, `/capabilities`, `/cache/*` routes |
| `src/mdflow/settings.py` | Pydantic-Settings env vars (MDFLOW_*) |
| `tests/__init__.py` | Test package marker |
| `tests/conftest.py` | Shared fixtures (tmp cache dir, sample bytes) |
| `tests/test_errors.py` | ErrorCode + exception tests |
| `tests/test_events.py` | Event model tests |
| `tests/test_format_detect.py` | Detection tests |
| `tests/test_url_fetch.py` | URL validation + SSRF + fetch tests |
| `tests/test_cache.py` | Disk cache tests |
| `tests/test_registry.py` | Registry dispatch tests |
| `tests/test_capabilities.py` | GPU detection / env override tests |
| `tests/test_concurrency.py` | Semaphore + pool tests |
| `tests/test_service.py` | ConversionService unit tests |
| `tests/converters/__init__.py` | Test subpackage |
| `tests/converters/test_base.py` | Protocol/dataclass tests |
| `tests/converters/test_text.py` | Text converter tests |
| `tests/api/__init__.py` | Test subpackage |
| `tests/api/test_app.py` | App boot + /healthz test |
| `tests/api/test_admin.py` | /capabilities, /cache/* tests |
| `tests/test_m0_smoke.py` | End-to-end smoke: ConversionService + /healthz |
| `tests/fixtures/sample.txt` | UTF-8 plain text fixture |
| `tests/fixtures/sample.md` | Markdown passthrough fixture |
| `tests/fixtures/sample.csv` | CSV → table fixture |

**Not in M0 scope (defer to later milestones):**
- `/convert` SSE handler → M1
- `mdflow/converters/{docx,pptx,xlsx,html,pdf,...}` → M1, M2, M3
- `mdflow/mcp/*` → M4
- `mdflow/cli.py`, `Dockerfile` → M5

---

## Task 1: Repo bootstrap (pyproject.toml + structure + smoke import)

**Files:**
- Create: `pyproject.toml`
- Create: `src/mdflow/__init__.py`
- Create: `src/mdflow/core/__init__.py`
- Create: `src/mdflow/converters/__init__.py`
- Create: `src/mdflow/runtime/__init__.py`
- Create: `src/mdflow/api/__init__.py`
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`
- Test: `tests/test_smoke_import.py`

- [ ] **Step 1: Write the failing smoke-import test**

Create `tests/test_smoke_import.py`:

```python
"""Smoke test: package importable, version exposed."""
import mdflow


def test_package_imports():
    assert hasattr(mdflow, "__version__")
    assert isinstance(mdflow.__version__, str)
    assert mdflow.__version__.count(".") >= 1
```

- [ ] **Step 2: Run test to verify it fails**

Run from repo root:
```bash
python -m pytest tests/test_smoke_import.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow'` (pytest collection error).

- [ ] **Step 3: Write `pyproject.toml`**

Create `pyproject.toml`:

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mdflow"
version = "0.0.1"
description = "Document-to-Markdown conversion gateway (HTTP API + MCP)"
requires-python = ">=3.11"
authors = [{ name = "Taeyoung Song", email = "restful3@gmail.com" }]
license = { text = "MIT" }
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.5",
    "pydantic-settings>=2.1",
    "httpx>=0.26",
    "python-magic>=0.4.27",
    "chardet>=5.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.1",
    "ruff>=0.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/mdflow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra -q --strict-markers"
asyncio_mode = "auto"
markers = [
    "gpu: requires CUDA GPU",
    "libreoffice: requires LibreOffice runtime",
    "integration: end-to-end smoke",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]  # rely on formatter

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["B011"]  # allow assert False patterns
```

- [ ] **Step 4: Create package skeleton files**

Create `src/mdflow/__init__.py`:

```python
"""mdflow — document-to-Markdown conversion gateway."""

__version__ = "0.0.1"
```

Create empty `__init__.py` files for subpackages:

```bash
mkdir -p src/mdflow/core src/mdflow/converters src/mdflow/runtime src/mdflow/api
touch src/mdflow/core/__init__.py src/mdflow/converters/__init__.py src/mdflow/runtime/__init__.py src/mdflow/api/__init__.py
touch tests/__init__.py
```

- [ ] **Step 5: Create `tests/conftest.py` with shared fixtures**

```python
"""Shared pytest fixtures for mdflow tests."""
from pathlib import Path

import pytest


@pytest.fixture
def fixtures_dir() -> Path:
    return Path(__file__).parent / "fixtures"


@pytest.fixture
def tmp_cache_dir(tmp_path: Path) -> Path:
    cache = tmp_path / "mdflow_cache"
    cache.mkdir()
    return cache
```

Create `tests/fixtures/` directory:
```bash
mkdir -p tests/fixtures
```

- [ ] **Step 6: Install dev dependencies and run the smoke test**

```bash
python -m pip install -e ".[dev]"
python -m pytest tests/test_smoke_import.py -v
```
Expected: `1 passed`.

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml src/ tests/
git commit -m "feat(m0): bootstrap pyproject + package structure + smoke import"
```

---

## Task 2: `ErrorCode` enum + `MdflowError` exception

**Files:**
- Create: `src/mdflow/core/errors.py`
- Test: `tests/test_errors.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_errors.py`:

```python
"""ErrorCode enum + MdflowError exception."""
import pytest

from mdflow.core.errors import ErrorCode, MdflowError


def test_error_code_members_exist():
    expected = {
        "UNSUPPORTED_FORMAT",
        "FORMAT_DETECT_FAILED",
        "INPUT_TOO_LARGE",
        "CONVERSION_FAILED",
        "LIBREOFFICE_UNAVAILABLE",
        "TIMEOUT",
        "CACHE_IO_ERROR",
        "INTERNAL",
        "URL_INVALID",
        "URL_BLOCKED",
        "URL_FETCH_FAILED",
        "URL_TIMEOUT",
        "URL_TOO_LARGE",
        "URL_REDIRECT_LIMIT",
        "URL_NON_2XX",
    }
    actual = {member.name for member in ErrorCode}
    assert expected.issubset(actual), f"missing: {expected - actual}"


def test_error_code_retryable_mapping():
    assert ErrorCode.CONVERSION_FAILED.retryable is True
    assert ErrorCode.TIMEOUT.retryable is True
    assert ErrorCode.CACHE_IO_ERROR.retryable is True
    assert ErrorCode.URL_FETCH_FAILED.retryable is True
    assert ErrorCode.URL_TIMEOUT.retryable is True
    assert ErrorCode.UNSUPPORTED_FORMAT.retryable is False
    assert ErrorCode.URL_INVALID.retryable is False
    assert ErrorCode.URL_BLOCKED.retryable is False
    assert ErrorCode.URL_TOO_LARGE.retryable is False
    assert ErrorCode.URL_REDIRECT_LIMIT.retryable is False
    assert ErrorCode.URL_NON_2XX.retryable is False


def test_mdflow_error_carries_code_and_message():
    err = MdflowError(ErrorCode.URL_BLOCKED, "private IP rejected: 10.0.0.5")
    assert err.code is ErrorCode.URL_BLOCKED
    assert "private IP rejected" in str(err)
    assert err.retryable is False


def test_mdflow_error_is_exception():
    with pytest.raises(MdflowError) as exc_info:
        raise MdflowError(ErrorCode.INTERNAL, "boom")
    assert exc_info.value.code is ErrorCode.INTERNAL
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_errors.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.errors'`.

- [ ] **Step 3: Implement `errors.py`**

Create `src/mdflow/core/errors.py`:

```python
"""Standard error codes and exception type for mdflow."""
from __future__ import annotations

from enum import Enum


class ErrorCode(Enum):
    UNSUPPORTED_FORMAT = ("UNSUPPORTED_FORMAT", False)
    FORMAT_DETECT_FAILED = ("FORMAT_DETECT_FAILED", False)
    INPUT_TOO_LARGE = ("INPUT_TOO_LARGE", False)
    CONVERSION_FAILED = ("CONVERSION_FAILED", True)
    LIBREOFFICE_UNAVAILABLE = ("LIBREOFFICE_UNAVAILABLE", False)
    TIMEOUT = ("TIMEOUT", True)
    CACHE_IO_ERROR = ("CACHE_IO_ERROR", True)
    INTERNAL = ("INTERNAL", False)
    URL_INVALID = ("URL_INVALID", False)
    URL_BLOCKED = ("URL_BLOCKED", False)
    URL_FETCH_FAILED = ("URL_FETCH_FAILED", True)
    URL_TIMEOUT = ("URL_TIMEOUT", True)
    URL_TOO_LARGE = ("URL_TOO_LARGE", False)
    URL_REDIRECT_LIMIT = ("URL_REDIRECT_LIMIT", False)
    URL_NON_2XX = ("URL_NON_2XX", False)

    def __new__(cls, code: str, retryable: bool) -> ErrorCode:
        obj = object.__new__(cls)
        obj._value_ = code
        obj._retryable = retryable
        return obj

    @property
    def retryable(self) -> bool:
        return self._retryable


class MdflowError(Exception):
    """Error with a structured code carried alongside the message."""

    def __init__(self, code: ErrorCode, message: str) -> None:
        super().__init__(f"[{code.value}] {message}")
        self.code = code
        self.message = message

    @property
    def retryable(self) -> bool:
        return self.code.retryable
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_errors.py -v
```
Expected: `4 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/errors.py tests/test_errors.py
git commit -m "feat(m0): ErrorCode enum + MdflowError exception"
```

---

## Task 3: Pydantic SSE event models

**Files:**
- Create: `src/mdflow/core/events.py`
- Test: `tests/test_events.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_events.py`:

```python
"""SSE event Pydantic models."""
import pytest
from pydantic import ValidationError

from mdflow.core.events import (
    Cached,
    Done,
    Error,
    Progress,
    Queued,
    Started,
)


def test_started_event_minimal():
    e = Started(converter="text-passthrough", gpu=False, sha256="a" * 64)
    assert e.converter == "text-passthrough"
    assert e.gpu is False
    assert e.sha256 == "a" * 64


def test_progress_event_clamps_pct_to_0_100():
    Progress(stage="parse", pct=42, detail="line 3")
    with pytest.raises(ValidationError):
        Progress(stage="parse", pct=-1, detail="bad")
    with pytest.raises(ValidationError):
        Progress(stage="parse", pct=101, detail="bad")


def test_queued_event_requires_reason_and_position():
    e = Queued(reason="gpu_busy", position=2)
    assert e.reason == "gpu_busy"
    assert e.position == 2


def test_cached_event_records_sha_and_timestamp():
    e = Cached(sha256="b" * 64, cached_at="2026-05-21T10:00:00Z")
    assert e.sha256 == "b" * 64
    assert e.cached_at.endswith("Z")


def test_done_event_carries_markdown_and_metadata():
    e = Done(markdown="# Hello", metadata={"converter": "text"}, assets=[])
    assert e.markdown == "# Hello"
    assert e.metadata["converter"] == "text"
    assert e.assets == []


def test_error_event_uses_code_string():
    e = Error(code="URL_BLOCKED", message="private IP", retryable=False)
    assert e.code == "URL_BLOCKED"
    assert e.retryable is False


def test_event_json_serialization_roundtrip():
    started = Started(converter="text", gpu=False, sha256="c" * 64)
    payload = started.model_dump_json()
    assert "text" in payload
    parsed = Started.model_validate_json(payload)
    assert parsed == started
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_events.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.events'`.

- [ ] **Step 3: Implement `events.py`**

Create `src/mdflow/core/events.py`:

```python
"""Pydantic event models for the SSE stream."""
from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field


class _EventBase(BaseModel):
    model_config = ConfigDict(extra="forbid")


class Started(_EventBase):
    converter: str
    gpu: bool
    sha256: str


class Queued(_EventBase):
    reason: str
    position: int


class Progress(_EventBase):
    stage: str
    pct: int = Field(ge=0, le=100)
    detail: str = ""


class Cached(_EventBase):
    sha256: str
    cached_at: str  # ISO-8601 string; v1 uses string for SSE simplicity


class Done(_EventBase):
    markdown: str
    metadata: dict[str, Any] = Field(default_factory=dict)
    assets: list[str] = Field(default_factory=list)


class Error(_EventBase):
    code: str
    message: str
    retryable: bool
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_events.py -v
```
Expected: `7 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/events.py tests/test_events.py
git commit -m "feat(m0): Pydantic SSE event models"
```

---

## Task 4: Settings (env vars) module

**Files:**
- Create: `src/mdflow/settings.py`
- Test: `tests/test_settings.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_settings.py`:

```python
"""Pydantic-Settings env vars (MDFLOW_*)."""
import pytest

from mdflow.settings import Settings


def test_settings_defaults(monkeypatch):
    for var in [
        "MDFLOW_FORCE_CPU",
        "MDFLOW_CACHE_DIR",
        "MDFLOW_MAX_INPUT_MB",
        "MDFLOW_MAX_URL_INPUT_MB",
        "MDFLOW_ALLOW_PRIVATE_URLS",
        "MDFLOW_URL_MAX_REDIRECTS",
        "MDFLOW_URL_CONNECT_TIMEOUT_S",
        "MDFLOW_URL_READ_TIMEOUT_S",
        "MDFLOW_URL_USER_AGENT",
    ]:
        monkeypatch.delenv(var, raising=False)

    s = Settings()
    assert s.force_cpu is False
    assert s.max_input_mb == 200
    assert s.max_url_input_mb == 200  # default matches max_input_mb
    assert s.allow_private_urls is False
    assert s.url_max_redirects == 5
    assert s.url_connect_timeout_s == 10.0
    assert s.url_read_timeout_s == 30.0
    assert s.url_user_agent.startswith("mdflow/")


def test_settings_env_override(monkeypatch):
    monkeypatch.setenv("MDFLOW_FORCE_CPU", "1")
    monkeypatch.setenv("MDFLOW_MAX_URL_INPUT_MB", "50")
    monkeypatch.setenv("MDFLOW_ALLOW_PRIVATE_URLS", "true")
    monkeypatch.setenv("MDFLOW_URL_MAX_REDIRECTS", "3")
    s = Settings()
    assert s.force_cpu is True
    assert s.max_url_input_mb == 50
    assert s.allow_private_urls is True
    assert s.url_max_redirects == 3


def test_max_url_input_must_not_exceed_max_input(monkeypatch):
    monkeypatch.setenv("MDFLOW_MAX_INPUT_MB", "100")
    monkeypatch.setenv("MDFLOW_MAX_URL_INPUT_MB", "200")
    with pytest.raises(ValueError):
        Settings()
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_settings.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.settings'`.

- [ ] **Step 3: Implement `settings.py`**

Create `src/mdflow/settings.py`:

```python
"""Environment-derived settings (MDFLOW_*)."""
from __future__ import annotations

from pathlib import Path

from pydantic import Field, model_validator
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_prefix="MDFLOW_",
        env_file=None,
        extra="ignore",
    )

    force_cpu: bool = Field(default=False)
    cache_dir: Path = Field(default=Path.home() / ".cache" / "mdflow")
    max_input_mb: int = Field(default=200, gt=0)
    max_url_input_mb: int = Field(default=200, gt=0)
    allow_private_urls: bool = Field(default=False)
    url_max_redirects: int = Field(default=5, ge=0)
    url_connect_timeout_s: float = Field(default=10.0, gt=0)
    url_read_timeout_s: float = Field(default=30.0, gt=0)
    url_user_agent: str = Field(default="mdflow/0.0.1 (+https://github.com/restful3/mdflow)")

    @model_validator(mode="after")
    def _check_url_input_cap(self) -> "Settings":
        if self.max_url_input_mb > self.max_input_mb:
            raise ValueError(
                f"MDFLOW_MAX_URL_INPUT_MB ({self.max_url_input_mb}) must be <= "
                f"MDFLOW_MAX_INPUT_MB ({self.max_input_mb})"
            )
        return self
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_settings.py -v
```
Expected: `3 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/settings.py tests/test_settings.py
git commit -m "feat(m0): Settings (MDFLOW_* env vars) with URL-input cap validation"
```

---

## Task 5: Format detection (extension + magic bytes)

**Files:**
- Create: `src/mdflow/core/format_detect.py`
- Test: `tests/test_format_detect.py`
- Test fixtures: `tests/fixtures/sample.txt`, `tests/fixtures/sample.md`

- [ ] **Step 1: Create fixtures**

```bash
printf '%s\n' "Hello, mdflow." > tests/fixtures/sample.txt
printf '# Sample\n\nContent.\n' > tests/fixtures/sample.md
```

- [ ] **Step 2: Write failing tests**

Create `tests/test_format_detect.py`:

```python
"""Format detection: extension + magic bytes agreement."""
from pathlib import Path

from mdflow.core.format_detect import DetectionResult, detect_format


def test_detect_txt_by_extension_only(fixtures_dir):
    data = (fixtures_dir / "sample.txt").read_bytes()
    result = detect_format(data, filename_hint="sample.txt")
    assert result.format == "txt"
    assert result.source in {"ext", "agreement"}
    assert result.warnings == []


def test_detect_pdf_by_magic_only():
    pdf_bytes = b"%PDF-1.4\n%\xe2\xe3\xcf\xd3\n"
    result = detect_format(pdf_bytes, filename_hint=None)
    assert result.format == "pdf"
    assert result.source == "magic"


def test_detect_disagreement_prefers_magic_and_warns():
    pdf_bytes = b"%PDF-1.4\n"
    result = detect_format(pdf_bytes, filename_hint="trick.txt")
    assert result.format == "pdf"
    assert any("disagreement" in w for w in result.warnings)


def test_detect_unknown_returns_failed():
    result = detect_format(b"\x00\x01\x02noisy", filename_hint=None)
    assert result.format is None
    assert result.source == "unknown"


def test_detection_result_dataclass():
    r = DetectionResult(format="pdf", source="magic", warnings=["x"])
    assert r.format == "pdf"
    assert r.source == "magic"
    assert r.warnings == ["x"]
```

- [ ] **Step 3: Run test to verify it fails**

```bash
python -m pytest tests/test_format_detect.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.format_detect'`.

- [ ] **Step 4: Implement `format_detect.py`**

Create `src/mdflow/core/format_detect.py`:

```python
"""Detect input format using extension + magic bytes."""
from __future__ import annotations

from dataclasses import dataclass, field
from pathlib import PurePosixPath

try:
    import magic  # python-magic
except ImportError:
    magic = None


@dataclass
class DetectionResult:
    format: str | None
    source: str  # "ext", "magic", "agreement", "unknown"
    warnings: list[str] = field(default_factory=list)


# Subset relevant to M0; extended by later milestones.
_MIME_TO_FORMAT: dict[str, str] = {
    "text/plain": "txt",
    "text/markdown": "md",
    "text/csv": "csv",
    "application/pdf": "pdf",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document": "docx",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation": "pptx",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": "xlsx",
    "text/html": "html",
    "application/xhtml+xml": "html",
}

_EXT_TO_FORMAT: dict[str, str] = {
    ".txt": "txt",
    ".md": "md",
    ".markdown": "md",
    ".csv": "csv",
    ".pdf": "pdf",
    ".docx": "docx",
    ".pptx": "pptx",
    ".xlsx": "xlsx",
    ".html": "html",
    ".htm": "html",
    ".hwp": "hwp",
    ".doc": "doc",
    ".ppt": "ppt",
}


def _ext_format(filename_hint: str | None) -> str | None:
    if not filename_hint:
        return None
    ext = PurePosixPath(filename_hint).suffix.lower()
    return _EXT_TO_FORMAT.get(ext)


def _magic_format(data: bytes) -> str | None:
    # Magic-bytes prefix probes — keep deterministic and dependency-free
    # so tests don't depend on the libmagic database.
    if data.startswith(b"%PDF"):
        return "pdf"
    if data[:4] == b"PK\x03\x04":
        # ZIP container — could be docx/pptx/xlsx. Best-effort look at
        # `word/`, `ppt/`, `xl/` paths inside.
        head = data[:4096]
        if b"word/" in head:
            return "docx"
        if b"ppt/" in head:
            return "pptx"
        if b"xl/" in head:
            return "xlsx"
        return None
    lowered = data[:1024].lstrip().lower()
    if lowered.startswith(b"<!doctype html") or lowered.startswith(b"<html"):
        return "html"
    # Fallback: libmagic if available
    if magic is not None:
        try:
            mime = magic.from_buffer(data, mime=True)
            return _MIME_TO_FORMAT.get(mime)
        except Exception:  # noqa: BLE001 — libmagic is best-effort
            return None
    return None


def detect_format(data: bytes, filename_hint: str | None) -> DetectionResult:
    ext = _ext_format(filename_hint)
    magic_fmt = _magic_format(data)
    warnings: list[str] = []

    if ext and magic_fmt:
        if ext == magic_fmt:
            return DetectionResult(format=ext, source="agreement", warnings=warnings)
        warnings.append(
            f"extension/magic disagreement: ext={ext}, magic={magic_fmt}; using magic"
        )
        return DetectionResult(format=magic_fmt, source="magic", warnings=warnings)
    if magic_fmt:
        return DetectionResult(format=magic_fmt, source="magic", warnings=warnings)
    if ext:
        return DetectionResult(format=ext, source="ext", warnings=warnings)
    return DetectionResult(format=None, source="unknown", warnings=warnings)
```

- [ ] **Step 5: Run test to verify it passes**

```bash
python -m pytest tests/test_format_detect.py -v
```
Expected: `5 passed`.

- [ ] **Step 6: Commit**

```bash
git add src/mdflow/core/format_detect.py tests/test_format_detect.py tests/fixtures/sample.txt tests/fixtures/sample.md
git commit -m "feat(m0): format detection with magic-bytes priority on disagreement"
```

---

## Task 6: Converter Protocol + Context/Result dataclasses

**Files:**
- Create: `src/mdflow/converters/base.py`
- Test: `tests/converters/__init__.py`, `tests/converters/test_base.py`

- [ ] **Step 1: Write failing tests**

Create `tests/converters/__init__.py` (empty), then `tests/converters/test_base.py`:

```python
"""Converter Protocol + Context/Result dataclasses."""
from pathlib import Path

from mdflow.converters.base import (
    ConversionContext,
    ConversionResult,
    Converter,
)


class _DummyConverter:
    name = "dummy"
    formats = ("txt",)
    requires_gpu = False

    def can_handle(self, ctx: ConversionContext) -> bool:
        return ctx.format == "txt"

    def convert(self, ctx, progress) -> ConversionResult:
        progress("done", 100)
        return ConversionResult(markdown="x", metadata={}, assets=[])


def test_dummy_satisfies_protocol():
    inst: Converter = _DummyConverter()
    assert inst.name == "dummy"
    assert inst.formats == ("txt",)
    assert inst.requires_gpu is False


def test_context_default_metadata_is_empty():
    ctx = ConversionContext(
        data=b"abc",
        filename_hint="x.txt",
        format="txt",
        options={},
        tmp_path=Path("/tmp/x"),
    )
    assert ctx.metadata == {}
    assert ctx.options == {}


def test_result_supports_assets_list():
    r = ConversionResult(markdown="# x", metadata={"k": 1}, assets=["a.png"])
    assert r.markdown == "# x"
    assert r.assets == ["a.png"]


def test_dummy_convert_calls_progress():
    seen: list[tuple[str, int]] = []

    def progress(stage: str, pct: int) -> None:
        seen.append((stage, pct))

    ctx = ConversionContext(
        data=b"hi", filename_hint="a.txt", format="txt", options={}, tmp_path=Path("/tmp/a")
    )
    inst = _DummyConverter()
    out = inst.convert(ctx, progress)
    assert out.markdown == "x"
    assert seen == [("done", 100)]
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/converters/test_base.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.converters.base'`.

- [ ] **Step 3: Implement `converters/base.py`**

Create `src/mdflow/converters/base.py`:

```python
"""Converter Protocol + supporting dataclasses."""
from __future__ import annotations

from dataclasses import dataclass, field
from pathlib import Path
from typing import Any, Callable, Protocol, runtime_checkable


ProgressCallback = Callable[[str, int], None]


@dataclass
class ConversionContext:
    """Input passed to a converter."""

    data: bytes
    filename_hint: str | None
    format: str
    options: dict[str, Any] = field(default_factory=dict)
    tmp_path: Path | None = None
    metadata: dict[str, Any] = field(default_factory=dict)


@dataclass
class ConversionResult:
    """Output produced by a converter; cached as-is."""

    markdown: str
    metadata: dict[str, Any] = field(default_factory=dict)
    assets: list[str] = field(default_factory=list)


@runtime_checkable
class Converter(Protocol):
    name: str
    formats: tuple[str, ...]
    requires_gpu: bool

    def can_handle(self, ctx: ConversionContext) -> bool: ...

    def convert(self, ctx: ConversionContext, progress: ProgressCallback) -> ConversionResult: ...
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/converters/test_base.py -v
```
Expected: `4 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/converters/base.py tests/converters/__init__.py tests/converters/test_base.py
git commit -m "feat(m0): Converter Protocol + ConversionContext/Result dataclasses"
```

---

## Task 7: TextConverter (txt/md/csv passthrough)

**Files:**
- Create: `src/mdflow/converters/text.py`
- Test: `tests/converters/test_text.py`
- Fixtures: `tests/fixtures/sample.csv`

- [ ] **Step 1: Create CSV fixture**

```bash
printf 'name,score\nalice,1\nbob,2\n' > tests/fixtures/sample.csv
```

- [ ] **Step 2: Write failing tests**

Create `tests/converters/test_text.py`:

```python
"""TextConverter: txt/md/csv passthrough + encoding detect."""
from pathlib import Path

from mdflow.converters.base import ConversionContext
from mdflow.converters.text import TextConverter


def _ctx(data: bytes, fmt: str, hint: str = "x") -> ConversionContext:
    return ConversionContext(
        data=data,
        filename_hint=hint,
        format=fmt,
        options={},
        tmp_path=Path("/tmp/x"),
    )


def test_txt_passthrough_utf8():
    conv = TextConverter()
    r = conv.convert(_ctx("hello\nworld".encode("utf-8"), "txt", "a.txt"), lambda s, p: None)
    assert r.markdown == "hello\nworld"
    assert r.metadata["encoding"] == "utf-8"


def test_txt_passthrough_cp949():
    text = "안녕"
    data = text.encode("cp949")
    conv = TextConverter()
    r = conv.convert(_ctx(data, "txt", "b.txt"), lambda s, p: None)
    assert "안녕" in r.markdown
    assert r.metadata["encoding"].lower() in {"cp949", "euc-kr"}


def test_md_passthrough_unchanged():
    md = "# Title\n\n- item\n"
    conv = TextConverter()
    r = conv.convert(_ctx(md.encode("utf-8"), "md", "c.md"), lambda s, p: None)
    assert r.markdown == md


def test_csv_renders_as_markdown_table(fixtures_dir):
    data = (fixtures_dir / "sample.csv").read_bytes()
    conv = TextConverter()
    r = conv.convert(_ctx(data, "csv", "sample.csv"), lambda s, p: None)
    assert "| name | score |" in r.markdown
    assert "| --- | --- |" in r.markdown
    assert "| alice | 1 |" in r.markdown


def test_can_handle_only_supported_formats():
    conv = TextConverter()
    ok = _ctx(b"", "txt")
    nope = _ctx(b"", "pdf")
    assert conv.can_handle(ok) is True
    assert conv.can_handle(nope) is False


def test_progress_called():
    seen: list[tuple[str, int]] = []
    conv = TextConverter()
    conv.convert(_ctx(b"hi", "txt", "x.txt"), lambda s, p: seen.append((s, p)))
    assert seen[-1] == ("done", 100)
```

- [ ] **Step 3: Run test to verify it fails**

```bash
python -m pytest tests/converters/test_text.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.converters.text'`.

- [ ] **Step 4: Implement `converters/text.py`**

Create `src/mdflow/converters/text.py`:

```python
"""txt/md/csv passthrough converter with encoding detection."""
from __future__ import annotations

import csv
import io

import chardet

from mdflow.converters.base import (
    ConversionContext,
    ConversionResult,
    ProgressCallback,
)


class TextConverter:
    name = "text-passthrough"
    formats = ("txt", "md", "csv")
    requires_gpu = False

    def can_handle(self, ctx: ConversionContext) -> bool:
        return ctx.format in self.formats

    def convert(self, ctx: ConversionContext, progress: ProgressCallback) -> ConversionResult:
        progress("decode", 10)
        text, encoding = _decode(ctx.data)

        if ctx.format == "csv":
            progress("render", 50)
            markdown = _csv_to_table(text)
        else:
            markdown = text

        progress("done", 100)
        return ConversionResult(
            markdown=markdown,
            metadata={"encoding": encoding},
            assets=[],
        )


def _decode(data: bytes) -> tuple[str, str]:
    try:
        return data.decode("utf-8"), "utf-8"
    except UnicodeDecodeError:
        detected = chardet.detect(data)
        encoding = (detected.get("encoding") or "latin-1").lower()
        try:
            return data.decode(encoding), encoding
        except (UnicodeDecodeError, LookupError):
            return data.decode("latin-1", errors="replace"), "latin-1"


def _csv_to_table(text: str) -> str:
    reader = csv.reader(io.StringIO(text))
    rows = list(reader)
    if not rows:
        return ""
    header, *body = rows
    out = ["| " + " | ".join(header) + " |"]
    out.append("| " + " | ".join("---" for _ in header) + " |")
    for row in body:
        # Pad short rows to header width
        padded = row + [""] * (len(header) - len(row))
        out.append("| " + " | ".join(padded[: len(header)]) + " |")
    return "\n".join(out)
```

- [ ] **Step 5: Run test to verify it passes**

```bash
python -m pytest tests/converters/test_text.py -v
```
Expected: `6 passed`.

- [ ] **Step 6: Commit**

```bash
git add src/mdflow/converters/text.py tests/converters/test_text.py tests/fixtures/sample.csv
git commit -m "feat(m0): TextConverter (txt/md/csv passthrough with encoding detect)"
```

---

## Task 8: Registry (decorator-based register + dispatch)

**Files:**
- Create: `src/mdflow/core/registry.py`
- Test: `tests/test_registry.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_registry.py`:

```python
"""Converter registry: decorator-based registration + dispatch."""
from pathlib import Path

import pytest

from mdflow.converters.base import ConversionContext, ConversionResult
from mdflow.core.errors import ErrorCode, MdflowError
from mdflow.core.registry import Registry


class _A:
    name = "a"
    formats = ("txt",)
    requires_gpu = False

    def can_handle(self, ctx): return ctx.format == "txt"

    def convert(self, ctx, progress):
        return ConversionResult(markdown="A", metadata={}, assets=[])


class _B:
    name = "b"
    formats = ("pdf",)
    requires_gpu = True

    def can_handle(self, ctx): return ctx.format == "pdf"

    def convert(self, ctx, progress):
        return ConversionResult(markdown="B", metadata={}, assets=[])


def _ctx(fmt: str) -> ConversionContext:
    return ConversionContext(data=b"", filename_hint=None, format=fmt, options={}, tmp_path=Path("/tmp/x"))


def test_register_and_select_by_format():
    reg = Registry()
    reg.register(_A())
    reg.register(_B())
    a = reg.select(_ctx("txt"))
    b = reg.select(_ctx("pdf"))
    assert a.name == "a"
    assert b.name == "b"


def test_select_unknown_format_raises():
    reg = Registry()
    with pytest.raises(MdflowError) as exc_info:
        reg.select(_ctx("xyz"))
    assert exc_info.value.code is ErrorCode.UNSUPPORTED_FORMAT


def test_first_registered_wins_for_same_format():
    reg = Registry()

    class _T1:
        name = "t1"; formats = ("txt",); requires_gpu = False
        def can_handle(self, ctx): return True
        def convert(self, ctx, progress): return ConversionResult(markdown="t1")

    class _T2:
        name = "t2"; formats = ("txt",); requires_gpu = False
        def can_handle(self, ctx): return True
        def convert(self, ctx, progress): return ConversionResult(markdown="t2")

    reg.register(_T1())
    reg.register(_T2())
    selected = reg.select(_ctx("txt"))
    assert selected.name == "t1"


def test_list_formats_aggregates_all_registered():
    reg = Registry()
    reg.register(_A())
    reg.register(_B())
    formats = reg.list_formats()
    names = {f["converter"] for f in formats}
    assert names == {"a", "b"}
    ext_to_gpu = {f["ext"]: f["requires_gpu"] for f in formats}
    assert ext_to_gpu == {"txt": False, "pdf": True}
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_registry.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.registry'`.

- [ ] **Step 3: Implement `core/registry.py`**

Create `src/mdflow/core/registry.py`:

```python
"""Converter registry — registration + format-based dispatch."""
from __future__ import annotations

from mdflow.converters.base import ConversionContext, Converter
from mdflow.core.errors import ErrorCode, MdflowError


class Registry:
    def __init__(self) -> None:
        self._converters: list[Converter] = []

    def register(self, converter: Converter) -> Converter:
        self._converters.append(converter)
        return converter

    def select(self, ctx: ConversionContext) -> Converter:
        for c in self._converters:
            if ctx.format in c.formats and c.can_handle(ctx):
                return c
        raise MdflowError(
            ErrorCode.UNSUPPORTED_FORMAT,
            f"no converter for format={ctx.format!r}",
        )

    def list_formats(self) -> list[dict]:
        out: list[dict] = []
        for c in self._converters:
            for ext in c.formats:
                out.append({"ext": ext, "converter": c.name, "requires_gpu": c.requires_gpu})
        return out
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_registry.py -v
```
Expected: `4 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/registry.py tests/test_registry.py
git commit -m "feat(m0): converter Registry (register + dispatch + list_formats)"
```

---

## Task 9: Cache (sha256 disk write + read + stats)

**Files:**
- Create: `src/mdflow/core/cache.py`
- Test: `tests/test_cache.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_cache.py`:

```python
"""Disk cache: sha256(input_bytes || canonical_options_json)."""
import json
from pathlib import Path

import pytest

from mdflow.converters.base import ConversionResult
from mdflow.core.cache import Cache, compute_cache_key


def test_compute_cache_key_is_deterministic():
    k1 = compute_cache_key(b"abc", {"a": 1, "b": 2})
    k2 = compute_cache_key(b"abc", {"b": 2, "a": 1})  # different dict order
    assert k1 == k2
    assert len(k1) == 64


def test_compute_cache_key_changes_with_options():
    k1 = compute_cache_key(b"abc", {"x": 1})
    k2 = compute_cache_key(b"abc", {"x": 2})
    assert k1 != k2


def test_cache_write_then_read(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    sha = "a" * 64
    res = ConversionResult(markdown="# x", metadata={"converter": "text"}, assets=[])
    cache.write(sha, res, options={"k": 1})
    got = cache.read(sha)
    assert got is not None
    assert got.markdown == "# x"
    assert got.metadata["converter"] == "text"


def test_cache_read_miss_returns_none(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    assert cache.read("f" * 64) is None


def test_cache_write_is_atomic(tmp_cache_dir: Path):
    """A failing write must not leave a half-written meta.json."""
    cache = Cache(tmp_cache_dir)
    sha = "b" * 64
    res = ConversionResult(markdown="x")
    cache.write(sha, res, options={})
    entry_dir = tmp_cache_dir / sha
    assert (entry_dir / "result.md").exists()
    assert (entry_dir / "meta.json").exists()
    meta = json.loads((entry_dir / "meta.json").read_text())
    assert meta["sha256"] == sha


def test_cache_stats_counts(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    sha = "c" * 64
    cache.write(sha, ConversionResult(markdown="y"), options={})
    cache.read(sha)        # hit
    cache.read("d" * 64)    # miss
    stats = cache.stats()
    assert stats["entries"] == 1
    assert stats["hit_count"] == 1
    assert stats["miss_count"] == 1


def test_cache_delete_removes_entry(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    sha = "e" * 64
    cache.write(sha, ConversionResult(markdown="z"), options={})
    assert cache.read(sha) is not None
    removed = cache.delete(sha)
    assert removed is True
    assert cache.read(sha) is None
    assert cache.delete(sha) is False  # second delete is a no-op


def test_cache_purge_clears_all(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    cache.write("1" * 64, ConversionResult(markdown="a"), options={})
    cache.write("2" * 64, ConversionResult(markdown="b"), options={})
    count = cache.purge()
    assert count == 2
    assert cache.stats()["entries"] == 0


def test_cache_invalid_sha_format_rejected(tmp_cache_dir: Path):
    cache = Cache(tmp_cache_dir)
    with pytest.raises(ValueError):
        cache.read("../etc/passwd")
    with pytest.raises(ValueError):
        cache.read("g" * 64)  # 'g' not hex
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_cache.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.cache'`.

- [ ] **Step 3: Implement `core/cache.py`**

Create `src/mdflow/core/cache.py`:

```python
"""sha256 disk cache with atomic writes and basic stats."""
from __future__ import annotations

import hashlib
import json
import os
import re
import shutil
from dataclasses import dataclass
from pathlib import Path
from typing import Any

from mdflow.converters.base import ConversionResult

_SHA256_RE = re.compile(r"^[0-9a-f]{64}$")


def compute_cache_key(data: bytes, options: dict[str, Any]) -> str:
    canonical = json.dumps(options, sort_keys=True, separators=(",", ":")).encode("utf-8")
    h = hashlib.sha256()
    h.update(data)
    h.update(b"\x00")
    h.update(canonical)
    return h.hexdigest()


def _validate_sha(sha: str) -> None:
    if not _SHA256_RE.match(sha):
        raise ValueError(f"invalid sha256: {sha!r}")


@dataclass
class _Stats:
    hit_count: int = 0
    miss_count: int = 0


class Cache:
    def __init__(self, root: Path) -> None:
        self.root = root
        self.root.mkdir(parents=True, exist_ok=True)
        self._stats = _Stats()

    def _entry_dir(self, sha: str) -> Path:
        _validate_sha(sha)
        return self.root / sha

    def write(self, sha: str, result: ConversionResult, options: dict[str, Any]) -> None:
        entry = self._entry_dir(sha)
        tmp = self.root / f".tmp-{sha}"
        if tmp.exists():
            shutil.rmtree(tmp)
        tmp.mkdir(parents=True)
        (tmp / "result.md").write_text(result.markdown, encoding="utf-8")
        meta = {
            "sha256": sha,
            "options": options,
            "metadata": result.metadata,
            "assets": result.assets,
        }
        (tmp / "meta.json").write_text(
            json.dumps(meta, ensure_ascii=False, indent=2), encoding="utf-8"
        )
        if entry.exists():
            shutil.rmtree(entry)
        os.replace(tmp, entry)

    def read(self, sha: str) -> ConversionResult | None:
        entry = self._entry_dir(sha)
        result_file = entry / "result.md"
        meta_file = entry / "meta.json"
        if not (result_file.exists() and meta_file.exists()):
            self._stats.miss_count += 1
            return None
        markdown = result_file.read_text(encoding="utf-8")
        meta = json.loads(meta_file.read_text(encoding="utf-8"))
        self._stats.hit_count += 1
        return ConversionResult(
            markdown=markdown,
            metadata=meta.get("metadata", {}),
            assets=meta.get("assets", []),
        )

    def delete(self, sha: str) -> bool:
        entry = self._entry_dir(sha)
        if not entry.exists():
            return False
        shutil.rmtree(entry)
        return True

    def purge(self) -> int:
        count = 0
        for child in self.root.iterdir():
            if child.is_dir() and _SHA256_RE.match(child.name):
                shutil.rmtree(child)
                count += 1
        return count

    def stats(self) -> dict[str, Any]:
        entries = sum(
            1 for c in self.root.iterdir() if c.is_dir() and _SHA256_RE.match(c.name)
        )
        size_mb = (
            sum(p.stat().st_size for p in self.root.rglob("*") if p.is_file())
            / 1_048_576
        )
        return {
            "entries": entries,
            "size_mb": round(size_mb, 3),
            "hit_count": self._stats.hit_count,
            "miss_count": self._stats.miss_count,
        }
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_cache.py -v
```
Expected: `9 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/cache.py tests/test_cache.py
git commit -m "feat(m0): sha256 disk cache (atomic write, stats, sha validation)"
```

---

## Task 10: Capabilities (GPU detect + boot log)

**Files:**
- Create: `src/mdflow/runtime/capabilities.py`
- Test: `tests/test_capabilities.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_capabilities.py`:

```python
"""Runtime capabilities: GPU detection + env override."""
import logging

from mdflow.runtime.capabilities import Capabilities, detect


def test_detect_force_cpu_overrides(monkeypatch):
    monkeypatch.setenv("MDFLOW_FORCE_CPU", "1")
    caps = detect()
    assert caps.gpu is False
    assert caps.cpu_workers >= 1


def test_capabilities_dataclass_basic():
    caps = Capabilities(gpu=False, cuda_version=None, cpu_workers=4)
    assert caps.gpu is False
    assert caps.cuda_version is None
    assert caps.cpu_workers == 4


def test_detect_without_torch_returns_no_gpu(monkeypatch):
    """When torch isn't installed, gpu must be False, never an error."""
    monkeypatch.delenv("MDFLOW_FORCE_CPU", raising=False)
    monkeypatch.setattr(
        "mdflow.runtime.capabilities._try_torch_cuda",
        lambda: (False, None),
    )
    caps = detect()
    assert caps.gpu is False
    assert caps.cuda_version is None


def test_boot_log_line_format(caplog, monkeypatch):
    monkeypatch.setenv("MDFLOW_FORCE_CPU", "1")
    with caplog.at_level(logging.INFO, logger="mdflow.runtime.capabilities"):
        Capabilities.log_boot(detect())
    assert any("mdflow ready" in r.message for r in caplog.records)
    msg = next(r.message for r in caplog.records if "mdflow ready" in r.message)
    assert "gpu=false" in msg
    assert "cpu_workers=" in msg
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_capabilities.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.runtime.capabilities'`.

- [ ] **Step 3: Implement `runtime/capabilities.py`**

Create `src/mdflow/runtime/capabilities.py`:

```python
"""Detect GPU + CPU runtime properties."""
from __future__ import annotations

import logging
import os
from dataclasses import dataclass

logger = logging.getLogger(__name__)


@dataclass
class Capabilities:
    gpu: bool
    cuda_version: str | None
    cpu_workers: int

    @staticmethod
    def log_boot(caps: "Capabilities") -> None:
        cuda = caps.cuda_version or "none"
        logger.info(
            "mdflow ready: gpu=%s cuda=%s cpu_workers=%d",
            str(caps.gpu).lower(),
            cuda,
            caps.cpu_workers,
        )


def _try_torch_cuda() -> tuple[bool, str | None]:
    try:
        import torch  # type: ignore
    except ImportError:
        return False, None
    try:
        if torch.cuda.is_available():
            return True, torch.version.cuda  # type: ignore[attr-defined]
        return False, None
    except Exception:  # noqa: BLE001
        return False, None


def detect() -> Capabilities:
    if os.environ.get("MDFLOW_FORCE_CPU", "").lower() in {"1", "true", "yes"}:
        gpu, cuda = False, None
    else:
        gpu, cuda = _try_torch_cuda()
    cpu_workers = max(1, os.cpu_count() or 1)
    return Capabilities(gpu=gpu, cuda_version=cuda, cpu_workers=cpu_workers)
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_capabilities.py -v
```
Expected: `4 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/runtime/capabilities.py tests/test_capabilities.py
git commit -m "feat(m0): Capabilities (GPU detect, env override, boot log line)"
```

---

## Task 11: Concurrency (GPU semaphore + CPU thread pool)

**Files:**
- Create: `src/mdflow/runtime/concurrency.py`
- Test: `tests/test_concurrency.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_concurrency.py`:

```python
"""Concurrency: GPU semaphore (=1) + CPU thread pool."""
import asyncio
import time
from concurrent.futures import ThreadPoolExecutor

from mdflow.runtime.concurrency import ConcurrencyPool


def test_pool_has_gpu_semaphore_and_cpu_executor():
    pool = ConcurrencyPool(cpu_workers=4)
    assert isinstance(pool.cpu_executor, ThreadPoolExecutor)
    assert pool.gpu_semaphore._value == 1


def test_gpu_semaphore_serializes_concurrent_acquirers():
    pool = ConcurrencyPool(cpu_workers=4)

    async def run() -> list[float]:
        timestamps: list[float] = []

        async def task() -> None:
            async with pool.gpu_lock():
                timestamps.append(time.monotonic())
                await asyncio.sleep(0.05)

        await asyncio.gather(task(), task(), task())
        return timestamps

    times = asyncio.run(run())
    # Each section must wait for the previous → gaps >= 0.04s
    gaps = [times[i + 1] - times[i] for i in range(len(times) - 1)]
    assert all(g >= 0.04 for g in gaps), f"gaps too small: {gaps}"


def test_pool_shutdown_clean():
    pool = ConcurrencyPool(cpu_workers=2)
    pool.shutdown()
    # shutdown twice must be a no-op
    pool.shutdown()
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_concurrency.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.runtime.concurrency'`.

- [ ] **Step 3: Implement `runtime/concurrency.py`**

Create `src/mdflow/runtime/concurrency.py`:

```python
"""Shared concurrency primitives: GPU semaphore + CPU thread pool."""
from __future__ import annotations

import asyncio
from concurrent.futures import ThreadPoolExecutor
from contextlib import asynccontextmanager


class ConcurrencyPool:
    """Holds a single GPU semaphore and a CPU thread pool for the process."""

    def __init__(self, cpu_workers: int) -> None:
        self.gpu_semaphore = asyncio.Semaphore(1)
        self.cpu_executor = ThreadPoolExecutor(
            max_workers=cpu_workers, thread_name_prefix="mdflow-cpu"
        )
        self._closed = False

    @asynccontextmanager
    async def gpu_lock(self):
        await self.gpu_semaphore.acquire()
        try:
            yield
        finally:
            self.gpu_semaphore.release()

    def shutdown(self) -> None:
        if self._closed:
            return
        self.cpu_executor.shutdown(wait=False, cancel_futures=True)
        self._closed = True
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_concurrency.py -v
```
Expected: `3 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/runtime/concurrency.py tests/test_concurrency.py
git commit -m "feat(m0): ConcurrencyPool (GPU semaphore + CPU thread pool)"
```

---

## Task 12: URL fetch helper (validation + SSRF + size-bounded GET)

**Files:**
- Create: `src/mdflow/core/url_fetch.py`
- Test: `tests/test_url_fetch.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_url_fetch.py`:

```python
"""URL fetch: validation + SSRF + redirect + size cap.

Implements PRD §5.0 (Claude-Codex agreement).
"""
import ipaddress
from unittest.mock import patch

import httpx
import pytest

from mdflow.core.errors import ErrorCode, MdflowError
from mdflow.core.url_fetch import (
    FetchResult,
    UrlPolicy,
    _is_blocked_ip,
    fetch_url,
    validate_url,
)


def test_validate_url_accepts_http_and_https():
    validate_url("http://example.com/x")
    validate_url("https://example.com/x")


def test_validate_url_rejects_unsupported_scheme():
    for bad in ["file:///etc/passwd", "ftp://x/", "gopher://x/", "javascript:alert(1)"]:
        with pytest.raises(MdflowError) as exc:
            validate_url(bad)
        assert exc.value.code is ErrorCode.URL_INVALID


def test_validate_url_rejects_missing_host():
    with pytest.raises(MdflowError) as exc:
        validate_url("https:///path")
    assert exc.value.code is ErrorCode.URL_INVALID


def test_validate_url_rejects_userinfo():
    with pytest.raises(MdflowError) as exc:
        validate_url("https://user:pw@example.com/")
    assert exc.value.code is ErrorCode.URL_INVALID


@pytest.mark.parametrize("addr", [
    "127.0.0.1",          # loopback
    "10.0.0.5",           # private
    "192.168.1.1",        # private
    "172.16.0.1",         # private
    "169.254.169.254",    # link-local / cloud metadata
    "224.0.0.1",          # multicast
    "0.0.0.0",            # unspecified
    "::1",                # IPv6 loopback
    "fd00::1",            # IPv6 ULA
    "fe80::1",            # IPv6 link-local
])
def test_is_blocked_ip_for_unsafe_addresses(addr):
    ip = ipaddress.ip_address(addr)
    assert _is_blocked_ip(ip) is True


@pytest.mark.parametrize("addr", ["8.8.8.8", "1.1.1.1", "2606:4700:4700::1111"])
def test_is_blocked_ip_allows_public(addr):
    ip = ipaddress.ip_address(addr)
    assert _is_blocked_ip(ip) is False


def _policy(allow_private: bool = False, max_redirects: int = 3, max_bytes: int = 1024) -> UrlPolicy:
    return UrlPolicy(
        allow_private_urls=allow_private,
        max_redirects=max_redirects,
        max_bytes=max_bytes,
        connect_timeout_s=1.0,
        read_timeout_s=1.0,
        user_agent="mdflow-test/0.0",
    )


def test_fetch_url_returns_metadata_on_2xx():
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(
            200,
            headers={"content-type": "text/plain"},
            content=b"hello",
        )

    transport = httpx.MockTransport(handler)
    res = fetch_url("https://example.com/x", _policy(), transport=transport)
    assert isinstance(res, FetchResult)
    assert res.data == b"hello"
    assert res.http_status == 200
    assert res.content_type == "text/plain"
    assert res.source_url == "https://example.com/x"
    assert res.effective_url == "https://example.com/x"
    assert res.redirect_count == 0


def test_fetch_url_size_cap_aborts_mid_stream():
    big_chunk = b"x" * 4096

    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=big_chunk)

    transport = httpx.MockTransport(handler)
    with pytest.raises(MdflowError) as exc:
        fetch_url("https://example.com/big", _policy(max_bytes=1024), transport=transport)
    assert exc.value.code is ErrorCode.URL_TOO_LARGE


def test_fetch_url_non_2xx_raises():
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(404, content=b"nope")

    transport = httpx.MockTransport(handler)
    with pytest.raises(MdflowError) as exc:
        fetch_url("https://example.com/404", _policy(), transport=transport)
    assert exc.value.code is ErrorCode.URL_NON_2XX


def test_fetch_url_redirect_followed_and_counted():
    def handler(request: httpx.Request) -> httpx.Response:
        if request.url.path == "/from":
            return httpx.Response(302, headers={"location": "https://example.com/to"})
        return httpx.Response(200, content=b"ok")

    transport = httpx.MockTransport(handler)
    res = fetch_url("https://example.com/from", _policy(max_redirects=3), transport=transport)
    assert res.data == b"ok"
    assert res.effective_url.endswith("/to")
    assert res.redirect_count == 1


def test_fetch_url_redirect_limit_exceeded_raises():
    def handler(request: httpx.Request) -> httpx.Response:
        # Always redirect onward
        path = request.url.path
        return httpx.Response(302, headers={"location": f"https://example.com{path}-x"})

    transport = httpx.MockTransport(handler)
    with pytest.raises(MdflowError) as exc:
        fetch_url("https://example.com/loop", _policy(max_redirects=2), transport=transport)
    assert exc.value.code is ErrorCode.URL_REDIRECT_LIMIT


def test_fetch_url_blocks_private_after_redirect():
    def handler(request: httpx.Request) -> httpx.Response:
        if request.url.host == "example.com":
            return httpx.Response(302, headers={"location": "http://127.0.0.1/secret"})
        return httpx.Response(200, content=b"oh no")

    transport = httpx.MockTransport(handler)
    with pytest.raises(MdflowError) as exc:
        fetch_url("https://example.com/r", _policy(), transport=transport)
    assert exc.value.code is ErrorCode.URL_BLOCKED


def test_fetch_url_user_agent_is_set():
    seen: list[str] = []

    def handler(request: httpx.Request) -> httpx.Response:
        seen.append(request.headers.get("user-agent", ""))
        return httpx.Response(200, content=b"x")

    transport = httpx.MockTransport(handler)
    fetch_url("https://example.com/", _policy(), transport=transport)
    assert seen == ["mdflow-test/0.0"]


def test_fetch_url_blocks_private_host_before_connect():
    # Skip DNS — pass an IP literal directly so the policy can decide pre-connect.
    with patch("mdflow.core.url_fetch._resolve_host") as resolve:
        resolve.return_value = [ipaddress.ip_address("10.0.0.5")]

        with pytest.raises(MdflowError) as exc:
            fetch_url("https://internal.example.com/", _policy())
        assert exc.value.code is ErrorCode.URL_BLOCKED


def test_fetch_url_allow_private_skips_block(monkeypatch):
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=b"ok")

    with patch("mdflow.core.url_fetch._resolve_host") as resolve:
        resolve.return_value = [ipaddress.ip_address("10.0.0.5")]
        transport = httpx.MockTransport(handler)
        res = fetch_url(
            "https://internal.example.com/",
            _policy(allow_private=True),
            transport=transport,
        )
        assert res.data == b"ok"
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_url_fetch.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.url_fetch'`.

- [ ] **Step 3: Implement `core/url_fetch.py`**

Create `src/mdflow/core/url_fetch.py`:

```python
"""URL fetch with PRD §5.0 policy: validation + SSRF + redirect + size cap."""
from __future__ import annotations

import datetime as _dt
import ipaddress
import socket
from dataclasses import dataclass, field
from typing import Any
from urllib.parse import urlparse, urlunparse

import httpx

from mdflow.core.errors import ErrorCode, MdflowError


@dataclass
class UrlPolicy:
    allow_private_urls: bool
    max_redirects: int
    max_bytes: int
    connect_timeout_s: float
    read_timeout_s: float
    user_agent: str


@dataclass
class FetchResult:
    data: bytes
    source_url: str
    effective_url: str
    http_status: int
    content_type: str | None
    content_length: int
    content_disposition: str | None
    filename_hint: str | None
    fetched_at: str
    redirect_count: int
    fetch_warnings: list[str] = field(default_factory=list)


# ---------- validation ----------

def validate_url(url: str) -> None:
    try:
        parsed = urlparse(url)
    except ValueError as e:
        raise MdflowError(ErrorCode.URL_INVALID, f"unparseable URL: {e}") from e

    if parsed.scheme not in {"http", "https"}:
        raise MdflowError(
            ErrorCode.URL_INVALID, f"unsupported scheme: {parsed.scheme!r}"
        )
    if not parsed.hostname:
        raise MdflowError(ErrorCode.URL_INVALID, "missing host")
    if parsed.username is not None or parsed.password is not None:
        raise MdflowError(ErrorCode.URL_INVALID, "userinfo not allowed in URL")


# ---------- SSRF ----------

def _is_blocked_ip(ip: ipaddress.IPv4Address | ipaddress.IPv6Address) -> bool:
    return (
        ip.is_loopback
        or ip.is_private
        or ip.is_link_local
        or ip.is_multicast
        or ip.is_unspecified
        or ip.is_reserved
    )


def _resolve_host(host: str) -> list[ipaddress.IPv4Address | ipaddress.IPv6Address]:
    """Resolve to a list of IP addresses (best-effort, blocking)."""
    try:
        infos = socket.getaddrinfo(host, None)
    except OSError as e:
        raise MdflowError(ErrorCode.URL_FETCH_FAILED, f"DNS resolution failed: {e}") from e
    out: list[ipaddress.IPv4Address | ipaddress.IPv6Address] = []
    for family, _, _, _, sockaddr in infos:
        addr = sockaddr[0]
        try:
            out.append(ipaddress.ip_address(addr))
        except ValueError:
            continue
    return out


def _enforce_ssrf(url: str, policy: UrlPolicy) -> None:
    parsed = urlparse(url)
    host = parsed.hostname or ""
    # If host is already an IP literal, use it directly.
    try:
        ip = ipaddress.ip_address(host)
        ips = [ip]
    except ValueError:
        ips = _resolve_host(host)

    if policy.allow_private_urls:
        return
    for ip in ips:
        if _is_blocked_ip(ip):
            raise MdflowError(
                ErrorCode.URL_BLOCKED,
                f"address {ip} for host {host!r} blocked by policy",
            )


# ---------- fetch ----------

def _content_disposition_filename(header: str | None) -> str | None:
    if not header:
        return None
    parts = [p.strip() for p in header.split(";")]
    for p in parts:
        if p.lower().startswith("filename="):
            value = p.split("=", 1)[1].strip().strip('"')
            return value or None
    return None


def _drop_fragment(url: str) -> str:
    p = urlparse(url)
    return urlunparse(p._replace(fragment=""))


def fetch_url(
    url: str,
    policy: UrlPolicy,
    *,
    transport: httpx.BaseTransport | None = None,
) -> FetchResult:
    source_url = _drop_fragment(url)
    validate_url(source_url)
    _enforce_ssrf(source_url, policy)

    timeout = httpx.Timeout(
        connect=policy.connect_timeout_s,
        read=policy.read_timeout_s,
        write=policy.read_timeout_s,
        pool=policy.connect_timeout_s,
    )
    headers = {"user-agent": policy.user_agent, "accept": "*/*"}

    current = source_url
    redirects = 0
    fetched_at = _dt.datetime.now(_dt.timezone.utc).isoformat().replace("+00:00", "Z")

    with httpx.Client(
        transport=transport,
        timeout=timeout,
        follow_redirects=False,
    ) as client:
        while True:
            try:
                with client.stream("GET", current, headers=headers) as resp:
                    if resp.is_redirect:
                        if redirects >= policy.max_redirects:
                            raise MdflowError(
                                ErrorCode.URL_REDIRECT_LIMIT,
                                f"exceeded max_redirects={policy.max_redirects}",
                            )
                        location = resp.headers.get("location")
                        if not location:
                            raise MdflowError(
                                ErrorCode.URL_FETCH_FAILED,
                                "redirect without Location header",
                            )
                        next_url = str(resp.url.join(location))
                        next_url = _drop_fragment(next_url)
                        validate_url(next_url)
                        _enforce_ssrf(next_url, policy)
                        current = next_url
                        redirects += 1
                        continue

                    if resp.status_code < 200 or resp.status_code >= 300:
                        raise MdflowError(
                            ErrorCode.URL_NON_2XX,
                            f"non-2xx status: {resp.status_code}",
                        )

                    data = bytearray()
                    for chunk in resp.iter_bytes():
                        data.extend(chunk)
                        if len(data) > policy.max_bytes:
                            raise MdflowError(
                                ErrorCode.URL_TOO_LARGE,
                                f"response exceeded max_bytes={policy.max_bytes}",
                            )

                    content_type = resp.headers.get("content-type")
                    cd = resp.headers.get("content-disposition")
                    filename_hint = _content_disposition_filename(cd)
                    if not filename_hint:
                        # last path segment as filename hint
                        path = urlparse(str(resp.url)).path
                        if path and not path.endswith("/"):
                            filename_hint = path.rsplit("/", 1)[-1]

                    return FetchResult(
                        data=bytes(data),
                        source_url=source_url,
                        effective_url=str(resp.url),
                        http_status=resp.status_code,
                        content_type=content_type,
                        content_length=len(data),
                        content_disposition=cd,
                        filename_hint=filename_hint,
                        fetched_at=fetched_at,
                        redirect_count=redirects,
                    )
            except httpx.TimeoutException as e:
                raise MdflowError(ErrorCode.URL_TIMEOUT, str(e)) from e
            except httpx.RequestError as e:
                raise MdflowError(ErrorCode.URL_FETCH_FAILED, str(e)) from e
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_url_fetch.py -v
```
Expected: `15 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/url_fetch.py tests/test_url_fetch.py
git commit -m "feat(m0): URL fetch helper (validation + SSRF + redirect + size cap)"
```

---

## Task 13: `ConversionService` (cache + dispatch wiring)

**Files:**
- Create: `src/mdflow/core/service.py`
- Test: `tests/test_service.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_service.py`:

```python
"""ConversionService: cache + format detect + dispatch wiring."""
from pathlib import Path

import pytest

from mdflow.converters.text import TextConverter
from mdflow.core.cache import Cache
from mdflow.core.errors import ErrorCode, MdflowError
from mdflow.core.registry import Registry
from mdflow.core.service import ConversionService, ConvertRequest


@pytest.fixture
def service(tmp_cache_dir: Path) -> ConversionService:
    reg = Registry()
    reg.register(TextConverter())
    return ConversionService(registry=reg, cache=Cache(tmp_cache_dir))


def test_convert_bytes_txt_returns_result(service: ConversionService):
    out = service.convert(ConvertRequest(data=b"hello", filename_hint="a.txt"))
    assert out.result.markdown == "hello"
    assert out.cached is False
    assert out.detected_format == "txt"


def test_convert_uses_cache_on_second_call(service: ConversionService):
    req = ConvertRequest(data=b"hello", filename_hint="a.txt")
    first = service.convert(req)
    second = service.convert(req)
    assert first.cached is False
    assert second.cached is True
    assert first.result.markdown == second.result.markdown


def test_convert_unsupported_format_raises(service: ConversionService):
    req = ConvertRequest(data=b"%PDF-1.4\n", filename_hint="x.pdf")
    with pytest.raises(MdflowError) as exc:
        service.convert(req)
    assert exc.value.code is ErrorCode.UNSUPPORTED_FORMAT


def test_convert_format_detect_failed_raises(service: ConversionService):
    req = ConvertRequest(data=b"\x00\x01garbage", filename_hint=None)
    with pytest.raises(MdflowError) as exc:
        service.convert(req)
    assert exc.value.code is ErrorCode.FORMAT_DETECT_FAILED


def test_convert_passes_options_into_cache_key(service: ConversionService):
    a = service.convert(ConvertRequest(data=b"hi", filename_hint="a.txt", options={"x": 1}))
    b = service.convert(ConvertRequest(data=b"hi", filename_hint="a.txt", options={"x": 2}))
    assert a.sha256 != b.sha256
    assert a.cached is False
    assert b.cached is False
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/test_service.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.core.service'`.

- [ ] **Step 3: Implement `core/service.py`**

Create `src/mdflow/core/service.py`:

```python
"""ConversionService — entry point that wires cache, detection, dispatch."""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from mdflow.converters.base import ConversionContext, ConversionResult
from mdflow.core.cache import Cache, compute_cache_key
from mdflow.core.errors import ErrorCode, MdflowError
from mdflow.core.format_detect import detect_format
from mdflow.core.registry import Registry


@dataclass
class ConvertRequest:
    data: bytes
    filename_hint: str | None
    options: dict[str, Any] = field(default_factory=dict)
    fetch_metadata: dict[str, Any] | None = None  # populated by URL fetch path


@dataclass
class ConvertResponse:
    result: ConversionResult
    sha256: str
    cached: bool
    detected_format: str
    converter_name: str


class ConversionService:
    def __init__(self, registry: Registry, cache: Cache) -> None:
        self.registry = registry
        self.cache = cache

    def convert(
        self,
        req: ConvertRequest,
        progress: callable = lambda stage, pct: None,
    ) -> ConvertResponse:
        sha = compute_cache_key(req.data, req.options)

        # Cache hit
        cached = self.cache.read(sha)
        if cached is not None:
            return ConvertResponse(
                result=cached,
                sha256=sha,
                cached=True,
                detected_format=cached.metadata.get("format", ""),
                converter_name=cached.metadata.get("converter", ""),
            )

        # Detect format
        detection = detect_format(req.data, req.filename_hint)
        if detection.format is None:
            raise MdflowError(
                ErrorCode.FORMAT_DETECT_FAILED,
                "extension and magic-bytes both unknown",
            )

        # Build context + dispatch
        ctx = ConversionContext(
            data=req.data,
            filename_hint=req.filename_hint,
            format=detection.format,
            options=req.options,
            metadata={"format": detection.format},
        )
        converter = self.registry.select(ctx)
        result = converter.convert(ctx, progress)

        # Augment result metadata with conversion info before caching
        enriched_meta = dict(result.metadata)
        enriched_meta.setdefault("converter", converter.name)
        enriched_meta.setdefault("format", detection.format)
        enriched_meta.setdefault("detection_source", detection.source)
        if detection.warnings:
            enriched_meta.setdefault("warnings", detection.warnings)
        result = ConversionResult(
            markdown=result.markdown, metadata=enriched_meta, assets=result.assets
        )

        self.cache.write(sha, result, options=req.options)
        return ConvertResponse(
            result=result,
            sha256=sha,
            cached=False,
            detected_format=detection.format,
            converter_name=converter.name,
        )
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/test_service.py -v
```
Expected: `5 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/core/service.py tests/test_service.py
git commit -m "feat(m0): ConversionService (cache + detect + dispatch wiring)"
```

---

## Task 14: FastAPI app factory + `/healthz` + lifespan

**Files:**
- Create: `src/mdflow/api/app.py`
- Test: `tests/api/__init__.py`, `tests/api/test_app.py`

- [ ] **Step 1: Write failing tests**

Create empty `tests/api/__init__.py`, then `tests/api/test_app.py`:

```python
"""FastAPI app boot + /healthz."""
from fastapi.testclient import TestClient

from mdflow.api.app import create_app


def test_healthz_returns_ok():
    app = create_app()
    with TestClient(app) as client:
        r = client.get("/healthz")
    assert r.status_code == 200
    body = r.json()
    assert body["ok"] is True
    assert "uptime_s" in body
    assert isinstance(body["uptime_s"], (int, float))
    assert body["uptime_s"] >= 0


def test_app_lifespan_initializes_state():
    app = create_app()
    with TestClient(app):
        # state attributes populated by lifespan
        assert hasattr(app.state, "capabilities")
        assert hasattr(app.state, "service")
        assert hasattr(app.state, "settings")
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/api/test_app.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.api.app'`.

- [ ] **Step 3: Implement `api/app.py`**

Create `src/mdflow/api/app.py`:

```python
"""FastAPI app factory."""
from __future__ import annotations

import logging
import time
from contextlib import asynccontextmanager

from fastapi import FastAPI

from mdflow.api.admin import register_admin_routes
from mdflow.converters.text import TextConverter
from mdflow.core.cache import Cache
from mdflow.core.registry import Registry
from mdflow.core.service import ConversionService
from mdflow.runtime.capabilities import Capabilities, detect
from mdflow.runtime.concurrency import ConcurrencyPool
from mdflow.settings import Settings

logger = logging.getLogger(__name__)


@asynccontextmanager
async def _lifespan(app: FastAPI):
    settings = Settings()
    capabilities = detect()
    Capabilities.log_boot(capabilities)

    registry = Registry()
    registry.register(TextConverter())

    cache = Cache(settings.cache_dir)
    pool = ConcurrencyPool(cpu_workers=capabilities.cpu_workers)
    service = ConversionService(registry=registry, cache=cache)

    app.state.started_at = time.monotonic()
    app.state.settings = settings
    app.state.capabilities = capabilities
    app.state.registry = registry
    app.state.cache = cache
    app.state.pool = pool
    app.state.service = service
    try:
        yield
    finally:
        pool.shutdown()


def create_app() -> FastAPI:
    app = FastAPI(title="mdflow", lifespan=_lifespan)

    @app.get("/healthz")
    def healthz() -> dict:
        uptime = time.monotonic() - app.state.started_at
        return {"ok": True, "uptime_s": uptime}

    register_admin_routes(app)
    return app
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/api/test_app.py -v
```
Expected: `2 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/api/app.py tests/api/__init__.py tests/api/test_app.py
git commit -m "feat(m0): FastAPI app factory + /healthz + lifespan-init Capabilities"
```

---

## Task 15: Admin endpoints (`/capabilities`, `/cache/*`)

**Files:**
- Create: `src/mdflow/api/admin.py`
- Test: `tests/api/test_admin.py`

- [ ] **Step 1: Write failing tests**

Create `tests/api/test_admin.py`:

```python
"""Admin endpoints: /capabilities, /cache/{sha256}, /cache/purge."""
from fastapi.testclient import TestClient

from mdflow.api.app import create_app
from mdflow.converters.base import ConversionResult


def test_capabilities_reports_gpu_and_formats(monkeypatch):
    monkeypatch.setenv("MDFLOW_FORCE_CPU", "1")
    app = create_app()
    with TestClient(app) as client:
        r = client.get("/capabilities")
    assert r.status_code == 200
    body = r.json()
    assert body["gpu"] is False
    assert body["cpu_workers"] >= 1
    assert any(f["ext"] == "txt" for f in body["formats"])
    assert "cache" in body
    assert body["cache"]["entries"] >= 0


def test_cache_get_unknown_returns_404():
    app = create_app()
    with TestClient(app) as client:
        r = client.get("/cache/" + "a" * 64)
    assert r.status_code == 404


def test_cache_get_invalid_sha_returns_400():
    app = create_app()
    with TestClient(app) as client:
        r = client.get("/cache/notasha")
    assert r.status_code == 400


def test_cache_write_then_get_returns_payload():
    app = create_app()
    with TestClient(app) as client:
        # Write a cache entry directly through the service for determinism
        service = app.state.service
        sha = service.cache.compute_key_for_test = "f" * 64
        service.cache.write(
            sha,
            ConversionResult(markdown="# hi", metadata={"converter": "text"}),
            options={},
        )
        r = client.get(f"/cache/{sha}")
    assert r.status_code == 200
    body = r.json()
    assert body["markdown"] == "# hi"
    assert body["metadata"]["converter"] == "text"


def test_cache_delete_then_get_returns_404():
    app = create_app()
    with TestClient(app) as client:
        service = app.state.service
        sha = "b" * 64
        service.cache.write(sha, ConversionResult(markdown="x"), options={})
        r1 = client.delete(f"/cache/{sha}")
        assert r1.status_code == 200
        assert r1.json() == {"ok": True}
        r2 = client.get(f"/cache/{sha}")
        assert r2.status_code == 404


def test_cache_purge_clears_all():
    app = create_app()
    with TestClient(app) as client:
        service = app.state.service
        service.cache.write("1" * 64, ConversionResult(markdown="a"), options={})
        service.cache.write("2" * 64, ConversionResult(markdown="b"), options={})
        r = client.post("/cache/purge")
    assert r.status_code == 200
    body = r.json()
    assert body["removed"] >= 2
```

- [ ] **Step 2: Run test to verify it fails**

```bash
python -m pytest tests/api/test_admin.py -v
```
Expected: `ModuleNotFoundError: No module named 'mdflow.api.admin'`.

- [ ] **Step 3: Implement `api/admin.py`**

Create `src/mdflow/api/admin.py`:

```python
"""Admin endpoints: /capabilities, /cache/*."""
from __future__ import annotations

from fastapi import FastAPI, HTTPException, Request


def register_admin_routes(app: FastAPI) -> None:
    @app.get("/capabilities")
    def capabilities(request: Request) -> dict:
        state = request.app.state
        return {
            "gpu": state.capabilities.gpu,
            "cuda_version": state.capabilities.cuda_version,
            "cpu_workers": state.capabilities.cpu_workers,
            "formats": state.registry.list_formats(),
            "cache": state.cache.stats(),
        }

    @app.get("/cache/{sha256}")
    def cache_get(sha256: str, request: Request) -> dict:
        try:
            entry = request.app.state.cache.read(sha256)
        except ValueError as e:
            raise HTTPException(status_code=400, detail=str(e)) from e
        if entry is None:
            raise HTTPException(status_code=404, detail="cache miss")
        return {
            "markdown": entry.markdown,
            "metadata": entry.metadata,
            "assets": entry.assets,
        }

    @app.delete("/cache/{sha256}")
    def cache_delete(sha256: str, request: Request) -> dict:
        try:
            removed = request.app.state.cache.delete(sha256)
        except ValueError as e:
            raise HTTPException(status_code=400, detail=str(e)) from e
        if not removed:
            raise HTTPException(status_code=404, detail="cache miss")
        return {"ok": True}

    @app.post("/cache/purge")
    def cache_purge(request: Request) -> dict:
        removed = request.app.state.cache.purge()
        return {"removed": removed}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
python -m pytest tests/api/test_admin.py -v
```
Expected: `6 passed`.

- [ ] **Step 5: Commit**

```bash
git add src/mdflow/api/admin.py tests/api/test_admin.py
git commit -m "feat(m0): admin endpoints (/capabilities, /cache/*)"
```

---

## Task 16: M0 smoke test (end-to-end skeleton verification)

**Files:**
- Test: `tests/test_m0_smoke.py`

- [ ] **Step 1: Write the integration smoke test**

Create `tests/test_m0_smoke.py`:

```python
"""End-to-end smoke for M0 chassis.

Exercises:
  - ConversionService direct call: text passthrough → ConvertResponse
  - Cache hit on repeat
  - URL fetch helper validates and rejects an SSRF target
  - FastAPI /healthz + /capabilities + /cache/* through TestClient
"""
import pytest
from fastapi.testclient import TestClient

from mdflow.api.app import create_app
from mdflow.core.errors import ErrorCode, MdflowError
from mdflow.core.service import ConvertRequest
from mdflow.core.url_fetch import UrlPolicy, validate_url


pytestmark = pytest.mark.integration


def test_m0_service_text_passthrough_then_cache(monkeypatch):
    monkeypatch.setenv("MDFLOW_FORCE_CPU", "1")
    app = create_app()
    with TestClient(app) as client:
        service = app.state.service
        req = ConvertRequest(data=b"hello mdflow", filename_hint="a.txt")
        r1 = service.convert(req)
        r2 = service.convert(req)
        assert r1.cached is False
        assert r2.cached is True
        assert r1.result.markdown == "hello mdflow"
        assert r2.sha256 == r1.sha256

        # /capabilities reflects the cache hit
        body = client.get("/capabilities").json()
        assert body["cache"]["hit_count"] >= 1


def test_m0_url_fetch_validates_and_blocks_ssrf():
    # validate_url alone catches scheme + userinfo + missing host
    with pytest.raises(MdflowError) as exc:
        validate_url("file:///etc/passwd")
    assert exc.value.code is ErrorCode.URL_INVALID


def test_m0_healthz_basic():
    app = create_app()
    with TestClient(app) as client:
        body = client.get("/healthz").json()
    assert body["ok"] is True
```

- [ ] **Step 2: Run test to verify it passes**

```bash
python -m pytest tests/test_m0_smoke.py -v -m integration
```
Expected: `3 passed`.

- [ ] **Step 3: Run the full suite and ensure everything is green**

```bash
python -m pytest -v
```
Expected: all tests pass, no warnings beyond DeprecationWarnings from third-party libs.

- [ ] **Step 4: Run ruff lint and fix any issues**

```bash
python -m ruff check src tests
python -m ruff format src tests
```

If lint reports issues, fix them, re-run, and ensure clean output before commit.

- [ ] **Step 5: Commit**

```bash
git add tests/test_m0_smoke.py
git commit -m "test(m0): integration smoke (service + cache + URL validate + /healthz)"
```

---

## Task 17: Tag and document M0 completion

**Files:**
- Modify: `STATE.md`

- [ ] **Step 1: Update STATE.md to reflect M0 done**

Edit `STATE.md` — replace the "## 1. 한눈에 보기" content to mark M0 complete and queue M1 as the next milestone. Use the exact text:

```markdown
## 1. 한눈에 보기

- **현재 단계**: M0 (skeleton) 구현 완료 → **M1 (사무 포맷) plan 작성 대기**
- **다음 액션**: 사용자 승인 후 `writing-plans` 스킬로 M1 plan 작성. M1 범위는 docx/pptx/xlsx/html 컨버터 + SSE `/convert` 스트림 + `convert_url` 통합
- **코드**: src layout, 17 모듈, 테스트 모두 통과, ruff clean
- **PRD**: 406줄, URL 처리 v1 정책 반영
```

- [ ] **Step 2: Run the full suite once more before tagging**

```bash
python -m pytest
```
Expected: all green.

- [ ] **Step 3: Commit STATE update + tag M0**

```bash
git add STATE.md
git commit -m "docs(m0): mark milestone complete in STATE.md"
git tag -a v0.0.1-m0 -m "M0 skeleton complete: core types, registry, cache, URL fetch, text passthrough, FastAPI /healthz + admin"
```

---

## Plan Self-Review

### Spec coverage (vs. PRD §14 M0 row + relevant sections)

| PRD requirement | Task(s) |
|---|---|
| `pyproject.toml` | Task 1 |
| `mdflow.core` modules | Tasks 2, 3, 5, 8, 9, 13 (+ url_fetch in 12) |
| `Converter` interface | Task 6 |
| `txt/md/csv` passthrough | Task 7 |
| `/healthz` | Task 14 |
| GPU detect / `Capabilities` (PRD §3) | Task 10 |
| GPU semaphore + CPU pool (PRD §3) | Task 11 |
| sha256 disk cache (PRD §9) | Task 9 |
| URL fetch + SSRF + redirect + size cap (PRD §5.0) | Task 12 |
| `URL_*` error codes (PRD §8.1) | Task 2 |
| `/capabilities`, `/cache/*` (PRD §6) | Task 15 |
| `MDFLOW_*` env vars (PRD §12) | Task 4 |
| Integration smoke | Task 16 |
| Milestone bookkeeping | Task 17 |

### Placeholder scan
No "TBD/TODO/fill in", no "add appropriate error handling" without code, no "similar to Task N". Every step shows actual code or an exact command.

### Type / name consistency
- `Converter` Protocol attrs (`name`, `formats`, `requires_gpu`, `can_handle`, `convert`) — defined in Task 6, used identically in Tasks 7, 8, 14, 15.
- `ConversionContext.format` / `ConversionContext.filename_hint` — Task 6 → 7 → 8 → 12 → 13.
- `Cache` API (`compute_cache_key`, `Cache.read/write/delete/purge/stats`) — Task 9 → 13 → 15.
- `ErrorCode.URL_*` enum members — Task 2 → 12.
- `Settings` field names (`force_cpu`, `cache_dir`, `max_url_input_mb`, etc.) — Task 4 → 14 (lifespan), 16.

### Notes for the executor
- Some converters in PRD §4 (`pdf`, `docx`, `pptx`, etc.) are intentionally **not** built in M0. M1 will plug them into the same `Registry` instantiated in `lifespan`.
- The URL fetch helper is implemented in M0 even though `/convert` SSE handler lands in M1 — this lets M1 wire `convert_url` without redesigning the fetch path.
- All tests use `httpx.MockTransport` or local fixtures; no real network calls.
- `python-magic` requires the libmagic system library. If CI lacks it, `_magic_format()` falls back to the in-line prefix probes (`%PDF`, `PK\\x03\\x04`, `<!DOCTYPE html>`), which cover the M0 fixtures. Document this in CI bring-up if it surfaces.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-21-m0-skeleton.md`. Two execution options:

**1. Subagent-Driven (recommended)** — Dispatch a fresh subagent per task, review between tasks, fast iteration. Uses `superpowers:subagent-driven-development`.

**2. Inline Execution** — Execute tasks in this session using `superpowers:executing-plans`. Batch execution with checkpoints for review.

Which approach?
